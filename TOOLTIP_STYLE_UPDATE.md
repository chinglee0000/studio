# Twin Matrix Tooltip 樣式更新

## ✅ 完成的修改

### 1. 移除綠色文字
- ❌ 移除了 `text-green-400` 的強度顯示
- ✅ 所有文字現在使用標準的主題顏色

### 2. 簡化 Tooltip 內容
只保留必要資訊：
- ✅ **標題**: Trait 名稱
- ✅ **分數**: 0-255 的數值（右對齊）
- ✅ **時間**: Unlocked 日期或 Locked 狀態

### 3. Progress Bar 樣式更新
- ✅ **維度名稱**: 使用黑色 (`text-foreground`)
- ✅ **百分比**: 移到進度條旁邊（右側）
- ✅ **數值**: `21/64` 靠右對齊
- ✅ **佈局**: 名稱在左，數值和百分比在右

---

## 🎨 新的 Tooltip 結構

### 已解鎖的 Trait
```
┌─────────────────────────┐
│ Gym Consistency         │
│ Score        20/255     │
│ Nov 4, 2024             │
└─────────────────────────┘
```

### 未解鎖的 Trait
```
┌─────────────────────────┐
│ Meditation Routine      │
│ Score       242/255     │
│ 🔒 Locked               │
└─────────────────────────┘
```

---

## 📊 Progress Bar 新佈局

```
Physical                    21/64  63%
████████████████░░░░░░░░

Social                      26/64  56%
████████████████░░░░░░░░

Digital                     21/64  55%
████████████████░░░░░░░░

Spiritual                   24/64  54%
████████████████░░░░░░░░
```

### 佈局說明
- **左側**: 維度名稱（黑色）
- **右側**: 數值 + 百分比
  - `21/64` (灰色)
  - `63%` (黑色)
- **下方**: 進度條

---

## 🔧 技術細節

### Tooltip 樣式
```tsx
<TooltipContent side="top" className="max-w-xs">
  <div className="space-y-1.5">
    {/* 標題 */}
    <p className="font-semibold text-sm">
      {trait.name || `Trait ${trait.id}`}
    </p>
    
    {/* 分數 (0-255) */}
    <div className="flex items-center justify-between text-xs">
      <span className="text-muted-foreground">Score</span>
      <span className="font-medium">{parseInt(trait.id, 16)}/255</span>
    </div>
    
    {/* 時間 */}
    {trait.discovered && trait.unlockedAt && (
      <div className="text-xs text-muted-foreground">
        {formatDate(trait.unlockedAt)}
      </div>
    )}
  </div>
</TooltipContent>
```

### Progress Bar 樣式
```tsx
<div className="flex items-center justify-between text-xs mb-1.5">
  {/* 左側：維度名稱 */}
  <span className="font-medium text-foreground">
    {label}
  </span>
  
  {/* 右側：數值 + 百分比 */}
  <div className="flex items-center gap-2">
    <span className="text-muted-foreground font-normal">
      {dim.discovered}/{dim.total}
    </span>
    <span className="font-medium text-foreground">
      {dim.percentage}%
    </span>
  </div>
</div>
```

---

## ✨ 改進效果

1. **更清晰**: 移除不必要的資訊，聚焦核心數據
2. **更易讀**: 黑色文字在所有主題下都清晰可見
3. **更簡潔**: Tooltip 更小更精簡
4. **更對齊**: Progress bar 的數值和百分比整齊排列

---

## 🎯 符合需求

- ✅ 不使用綠色文字
- ✅ Tooltip 只包含標題、分數、時間
- ✅ Progress bar 文字使用黑色
- ✅ 百分比在進度條旁邊
- ✅ 數值部分靠右對齊

所有修改已完成並通過編譯！
